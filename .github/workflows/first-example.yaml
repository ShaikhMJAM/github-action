name: My first workflow

on: push

jobs:
  first_job:
    runs-on: self-hosted
    steps:
      - name: Welcome Message
        run: echo "My First Github Actions Job"
      - name: list files
        run: ls
      - name: read files
        run: cat README.md
